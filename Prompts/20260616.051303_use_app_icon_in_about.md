# User Prompt
In the About page, use App icon as the image shown

## Description of Changes
Updated `activity_about.xml` to change the ImageView's source from `@mipmap/ic_launcher_round` to the actual app icon drawable `@drawable/ic_launcher`.
